{
  "name": "ai-multi-instance",
  "version": "2.0.1",
  "private": true,
  "description": "Local dashboard for running Claude Code, Codex, Cursor Agent, and custom CLI sessions in parallel with tmux persistence",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "node scripts/with-writable-tmpdir.mjs concurrently --kill-others-on-fail -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "node scripts/with-writable-tmpdir.mjs npm run start -w server",
    "test": "npm run test -w server && npm run test -w web",
    "service:install": "bash scripts/install-service.sh",
    "service:uninstall": "bash scripts/uninstall-service.sh",
    "service:start": "launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.ai-multi-instance.server.plist",
    "service:stop": "launchctl bootout gui/$(id -u)/com.ai-multi-instance.server",
    "service:restart": "launchctl kickstart -k gui/$(id -u)/com.ai-multi-instance.server",
    "service:status": "launchctl print gui/$(id -u)/com.ai-multi-instance.server",
    "service:log": "tail -f data/server.log",
    "tunnel:setup": "bash scripts/setup-named-tunnel.sh",
    "tunnel:install": "bash scripts/install-tunnel-service.sh",
    "tunnel:uninstall": "bash scripts/uninstall-tunnel-service.sh",
    "tunnel:start": "mkdir -p data/named-tunnel && touch data/named-tunnel/enabled && launchctl kickstart gui/$(id -u)/com.ai-multi-instance.tunnel",
    "tunnel:stop": "rm -f data/named-tunnel/enabled && launchctl kill SIGTERM gui/$(id -u)/com.ai-multi-instance.tunnel 2>/dev/null; true",
    "tunnel:restart": "mkdir -p data/named-tunnel && touch data/named-tunnel/enabled && launchctl kickstart -k gui/$(id -u)/com.ai-multi-instance.tunnel",
    "tunnel:status": "launchctl print gui/$(id -u)/com.ai-multi-instance.tunnel",
    "tunnel:log": "tail -f data/cloudflared.log"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.11"
  }
}
